// Enunciado: Desenvolva um programa que utilize um laço `for` para imprimir os números de 0 a 20, incrementando de 2 em 2, um em cada linha. 8/

for (int a = 0; a <= 20; a+=2)
{
    Console.WriteLine(a);
}